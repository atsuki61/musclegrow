// バリデーション用関数
// Zodを使ったスキーマ検証などをここで行う

/**
 * 例：トレーニング記録のバリデーション
 * import { z } from "zod";
 * 
 * export const workoutSchema = z.object({
 *   exercise: z.string().min(1),
 *   weight: z.number().min(0),
 *   reps: z.number().min(1),
 *   sets: z.number().min(1),
 * });
 */

